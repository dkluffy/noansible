---

tasks:

  - name: copy file
    shell: echo 1 >>/tmp/task1.txt
    plugin:  
      mod: file
      args: "@/tmp/task.txt, /tmp/task1.txt"

  - name: delete aaaa
    shell: rm -rf /tmp/aaaa

  - name: copy file2
    plugin:  
      mod: file
      args: ../files/b.yml, /tmp/aaaaaa.yml

  - name: task-ls aaaa
    shell: ls /tmp/aaaa
unkonwtag: unknow