---
hosts: TT
username: root

vars:
  pkdir: /root/pkc
  tmpdir: "/tmp/abctemp"

tasks:

  - name: task1-echo  1
    shell: echo task1-echo >>/tmp/task.txt
  
  - name: task2.1
    shell: date >>/tmp/task.txt

  - name: task2.2
    plugin:
      mod: synctime

  - name: task2.3
    shell: date >>/tmp/task.txt

  - include: a.yml

  - name: task-fail
    shell: echo fail >>/tmp/task.txt
    plugin:  
      mod: parted
      args: -d 1 -m 1
      
  - name: task3-echo 
    shell: echo task3-neverrun >>/tmp/task.txt
  